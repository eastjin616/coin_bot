# 2026-04-09 Strategy Improvements

상태: `completed`

## 목표

소액 시드 기준으로 전략-실행-표시 계층을 같은 기준으로 맞추고, 개선 항목을 체크리스트로 추적한다.

## 체크리스트

- [x] 실운영 신호를 마지막 확정 일봉 기준으로 정렬
- [x] 같은 확정 일봉의 중복 BUY/SELL 방지 (`signal_locks`)
- [x] 텔레그램 `/status` 와 런타임 상태를 실행 로직과 같은 RSI 기준으로 정렬
- [x] 런타임 유니버스 선정을 임계값 통과 방식에서 점수 기반 상위 N 선발로 전환
- [x] 장기 미청산 포지션용 time stop 규칙 추가 검토
- [x] 최근 실전 성과 기반 종목 자동 디레이팅 검토

## 진행 로그

### 완료

- 확정 일봉 기준 RSI 신호 적용
- `signal_locks` 테이블 추가 및 EC2 반영 완료
- 런타임 유니버스 자동선정을 점수 기반 상위 3 선발로 전환
- 최신 자동선정 결과 반영: `BCH`, `LINK`, `ADA`
- `max_hold_days` + `time_stop_min_pnl_pct` 기반 기간청산 추가
- 최근 30일 실전 성과 기반 신규매수 자동 디레이팅 추가

### 현재 작업

- 현재 단계 완료

### 메모

- 트레일링/손절은 실시간 현재가 기준을 유지한다.
- 표시용 숫자와 실행용 숫자가 다르면 운영 판단이 왜곡되므로 우선적으로 제거한다.
